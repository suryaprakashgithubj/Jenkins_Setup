pipeline {
    agent any

    stages {

        stage('Clone') {
            steps {
                git 'https://github.com/suryaprakashgithubj/Jenkins_Setup.git'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean test'
            }
        }

    }
}